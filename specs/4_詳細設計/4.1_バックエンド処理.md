# バックエンド処理

> バージョン: 1 | 更新日時: 2026/6/9 12:38:00

## [BP-001] 排他制御・プラットフォーム起動管理

- **備考:** プラットフォームがPicoかどうかは、machineモジュールのインポート可否によって判定される。
- **カテゴリ:** 起動管理
**security:**

- auth: 不要
- permissions: 

**operations:**

- **排他制御による二重起動防止**
  - type: BATCH
  - トリガー: Daemonモード起動時
- **MicroPython Wi-Fi接続**
  - type: BATCH
  - トリガー: Pico環境での起動時

- **説明:** Daemonモードでの起動時にプロセスの多重起動を防止し、またPico環境でのWi-Fi自動接続など、各プラットフォームに適した初期化を行う。
**relatedTables:**


**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/main.py
- mdns_proxy/src/wifi_manager.py


## [BP-002] データベース初期化管理

- **備考:** static_hosts への ip_address カラムがない場合は、後方互換性マイグレーションが自動的に行われる。
- **カテゴリ:** データベース管理
**security:**

- auth: 不要
- permissions: 

**operations:**

- **データベース初期化とWALモード設定**
  - type: BATCH
  - トリガー: アプリケーション起動時

- **説明:** SQLite3データベース（mdns_proxy.sqlite3）を構築し、各管理テーブルの初期化および最適化（WALモード・同期モード）を設定する。
**relatedTables:**

- static_hosts
- self_records
- other_records
- merged_records

**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/database.py


## [BP-003] 対話型CLI管理

- **備考:** 対話CLI実行中はログがコンソールを妨害しないよう、一時的にconsole_loggingが無効化される。
- **カテゴリ:** 管理者ツール
**security:**

- auth: 不要
- permissions: 

**operations:**

- **マージ済みレコードの表示**
  - type: READ
  - トリガー: CLIメニュー1の選択
- **静的ホストの表示**
  - type: READ
  - トリガー: CLIメニュー2の選択
- **静的ホストの追加**
  - type: CREATE
  - トリガー: CLIメニュー3の選択
- **静的ホストの削除**
  - type: DELETE
  - トリガー: CLIメニュー4の選択

**relatedTables:**

- static_hosts
- merged_records

**relatedScreens:**

- 対話型CLI画面
- マージ済みレコード表示画面
- 静的ホスト一覧画面
- 静的ホスト追加画面
- 静的ホスト削除画面

**sourceEvidence:**

- mdns_proxy/src/cli.py


## [BP-004] レコード同期API管理

- **備考:** ポート番号は system.ini の system.port （デフォルト80）で待ち受ける。
- **カテゴリ:** API配信
**security:**

- auth: トークン認証
- permissions: 

**operations:**

- **マージ済みレコード配信**
  - type: READ
  - トリガー: HTTP GET /api/merged-records
- **他プロキシからの同期情報受信**
  - type: CREATE
  - トリガー: HTTP POST /api/other-records

**relatedTables:**

- merged_records
- other_records

**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/api_server.py


## [BP-005] 独自DNS名前解決管理

- **カテゴリ:** 名前解決
**security:**

- auth: 不要
- permissions: 

**operations:**

- **静的ホスト名前解決**
  - type: CREATE
  - トリガー: スケジューラからの定期呼び出し

**relatedTables:**

- static_hosts
- self_records

**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/dns_resolver.py


## [BP-006] mDNSマルチキャストクエリ応答管理

- **備考:** ソケット生成時は SO_REUSEADDR および SO_REUSEPORT を設定し、マルチキャストグループ 224.0.0.251 に参加する。
- **カテゴリ:** 名前解決
**security:**

- auth: 不要
- permissions: 

**operations:**

- **mDNSマルチキャストクエリ応答**
  - type: SEARCH
  - トリガー: UDP 5353ポートでのmDNSクエリ受信

**relatedTables:**

- merged_records
- static_hosts

**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/mdns_server.py


## [BP-007] 常駐スケジュールタスク管理

- **備考:** このスケジューラはデーモンモード起動時に別スレッド等ではなく、メインのイベントループとして時系列に実行される。
- **カテゴリ:** 常駐処理
**security:**

- auth: 不要
- permissions: 

**operations:**

- **常駐スケジュール処理**
  - type: BATCH
  - トリガー: 設定インターバル(interval)周期でのループ実行

**relatedTables:**

- self_records
- other_records
- merged_records
- static_hosts

**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/scheduler.py


## [BP-008] ログローテーション管理

- **備考:** TimedRotatingFileHandler を拡張した ZipRotatingFileHandler により自動化されている。
- **カテゴリ:** システム管理
**security:**

- auth: 不要
- permissions: 

**operations:**

- **ログファイルのタイムスタンプローテーションとZIP自動圧縮**
  - type: BATCH
  - トリガー: ロガーの出力インターバル経過（日次など）

**relatedTables:**


**relatedScreens:**


**sourceEvidence:**

- mdns_proxy/src/logger_config.py

