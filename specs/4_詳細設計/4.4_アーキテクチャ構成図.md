# アーキテクチャ構成図

> バージョン: 2 | 更新日時: 2026/3/21 12:37:51

分散型mDNSプロキシによるセグメント間名前解決システム（DNS依存なし独自名前解決対応）

**クライアント層:**
- システム管理者
- mDNSクライアント

**プレゼンテーション層:**
- CLI管理インタフェース [Python CLI]

**ゲートウェイ層:**
- mDNSリスナー [UDP/5353]
- HTTPゲートウェイ [HTTP/TCP80]

**アプリケーション層:**
- Pico mDNSプロキシ [MicroPython]
- Windows mDNSプロキシ [Python EXE]
- 定期実行スケジューラ [Python Timer]
- 独自名前解決サービス [dig/Resolve-DnsName/ping/arping相当]
- プロキシ発見サービス [HTTP Client]
- トークン認証サービス [MAC Address Auth]
- JSONシリアライザ [JSON Parser]
- mDNS応答サービス [mDNS Protocol]
- レコードマージサービス [Data Merge Logic]
- TTL管理サービス [Time-based Cleanup]

**データ層:**
- SQLite3データベース [SQLite3]
  - static_hosts, self_records, other_records, merged_records, other_proxies, dns_resolution_log 等を管理
- 設定ファイル [INI Files]
- ログファイル [Text Files]

**外部サービス層:**
- 他セグメントプロキシ [HTTP API: /api/other-records, /api/static-hosts 等] （※実装に合わせて仕様変更：/api/static-hostsを外部同期対象に追加）
- Wi-Fiネットワーク [Wi-Fi 802.11]
- 解決対象ホスト [Network Hosts]

**接続:**
- システム管理者 → CLI管理インタフェース (Terminal)
- mDNSクライアント → mDNSリスナー (mDNS)
- CLI管理インタフェース → Pico mDNSプロキシ (Local API)
- CLI管理インタフェース → Windows mDNSプロキシ (Local API)
- mDNSリスナー → Pico mDNSプロキシ (UDP)
- mDNSリスナー → Windows mDNSプロキシ (UDP)
- Pico mDNSプロキシ → mDNS応答サービス (Internal)
- Windows mDNSプロキシ → mDNS応答サービス (Internal)
- mDNS応答サービス → mDNSクライアント (mDNS)
- HTTPゲートウェイ → Pico mDNSプロキシ (HTTP)
- HTTPゲートウェイ → Windows mDNSプロキシ (HTTP)
- Pico mDNSプロキシ → 定期実行スケジューラ (Internal)
- Windows mDNSプロキシ → 定期実行スケジューラ (Internal)
- 定期実行スケジューラ → 独自名前解決サービス (Internal)
- 定期実行スケジューラ → プロキシ発見サービス (Internal)
- 定期実行スケジューラ → レコードマージサービス (Internal)
- 定期実行スケジューラ → TTL管理サービス (Internal)
- Pico mDNSプロキシ → トークン認証サービス (Internal)
- Windows mDNSプロキシ → トークン認証サービス (Internal)
- Pico mDNSプロキシ → JSONシリアライザ (Internal)
- Windows mDNSプロキシ → JSONシリアライザ (Internal)
- レコードマージサービス → SQLite3データベース (SQLite)
- mDNS応答サービス → SQLite3データベース (SQLite)
- TTL管理サービス → SQLite3データベース (SQLite)
- Pico mDNSプロキシ → SQLite3データベース (SQLite)
- Windows mDNSプロキシ → SQLite3データベース (SQLite)
- Pico mDNSプロキシ → 設定ファイル (File I/O)
- Windows mDNSプロキシ → 設定ファイル (File I/O)
- Pico mDNSプロキシ → ログファイル (File I/O)
- Windows mDNSプロキシ → ログファイル (File I/O)
- 独自名前解決サービス → 解決対象ホスト (dig/resolve_dns_name/ping/arping相当)
- プロキシ発見サービス → 他セグメントプロキシ (HTTP)
- HTTPゲートウェイ → 他セグメントプロキシ (HTTP)
- Pico mDNSプロキシ → Wi-Fiネットワーク (Wi-Fi)
- Windows mDNSプロキシ → Wi-Fiネットワーク (Ethernet/Wi-Fi)

```mermaid
flowchart TD
    subgraph client["クライアント層"]
        user_admin["システム管理者"]
        user_client["mDNSクライアント"]
    end
    subgraph presentation["プレゼンテーション層"]
        cli["CLI管理インタフェース (Python CLI)"]
    end
    subgraph gateway["ゲートウェイ層"]
        mdns_listener["mDNSリスナー (UDP/5353)"]
        http_gateway["HTTPゲートウェイ (HTTP/TCP80)"]
    end
    subgraph application["アプリケーション層"]
        pico_proxy["Pico mDNSプロキシ (MicroPython)"]
        win_proxy["Windows mDNSプロキシ (Python EXE)"]
        scheduler["定期実行スケジューラ (Python Timer)"]
        dns_resolver["独自名前解決サービス (dig/Resolve-DnsName/ping/arping相当)"]
        proxy_discovery["プロキシ発見サービス (HTTP Client)"]
        token_auth["トークン認証サービス (MAC Address Auth)"]
        json_serializer["JSONシリアライザ (JSON Parser)"]
        mdns_responder["mDNS応答サービス (mDNS Protocol)"]
        record_merger["レコードマージサービス (Data Merge Logic)"]
        ttl_manager["TTL管理サービス (Time-based Cleanup)"]
    end
    subgraph data["データ層"]
        sqlite_db[("SQLite3データベース (SQLite3)")]
        table_other_proxies[("other_proxiesテーブル")]
        table_other_records[("other_recordsテーブル")]
        config_files["設定ファイル (INI Files)"]
        log_files["ログファイル (Text Files)"]
    end
    subgraph external["外部サービス層"]
        other_proxies["他セグメントプロキシ (HTTP API: /api/other-records, /api/static-hosts)"]
        wifi_network["Wi-Fiネットワーク (Wi-Fi 802.11)"]
        target_hosts["解決対象ホスト (Network Hosts)"]
    end
    user_admin -->|"設定管理"|cli
    user_client -->|"名前解決問い合わせ"|mdns_listener
    cli -->|"設定変更"|pico_proxy
    cli -->|"設定変更"|win_proxy
    mdns_listener -->|"mDNS問い合わせ"|pico_proxy
    mdns_listener -->|"mDNS問い合わせ"|win_proxy
    pico_proxy -->|"応答処理"|mdns_responder
    win_proxy -->|"応答処理"|mdns_responder
    mdns_responder -->|"名前解決応答"|user_client
    http_gateway -->|"プロキシ間通信"|pico_proxy
    http_gateway -->|"プロキシ間通信"|win_proxy
    pico_proxy -->|"定期処理制御"|scheduler
    win_proxy -->|"定期処理制御"|scheduler
    scheduler -->|"独自名前解決実行"|dns_resolver
    scheduler -->|"プロキシ発見実行"|proxy_discovery
    scheduler -->|"レコードマージ実行"|record_merger
    scheduler -->|"TTL管理実行"|ttl_manager
    pico_proxy -->|"トークン認証"|token_auth
    win_proxy -->|"トークン認証"|token_auth
    pico_proxy -->|"データ変換"|json_serializer
    win_proxy -->|"データ変換"|json_serializer
    record_merger -->|"マージレコード管理"|sqlite_db
    mdns_responder -->|"レコード検索"|sqlite_db
    ttl_manager -->|"期限切れレコード削除"|sqlite_db
    pico_proxy -->|"データ管理"|sqlite_db
    win_proxy -->|"データ管理"|sqlite_db
    table_other_proxies -->|"source/origin"|table_other_records
    pico_proxy -->|"データ管理"|table_other_proxies
    win_proxy -->|"データ管理"|table_other_proxies
    pico_proxy -->|"データ管理"|table_other_records
    win_proxy -->|"データ管理"|table_other_records
    pico_proxy -->|"設定読み込み"|config_files
    win_proxy -->|"設定読み込み"|config_files
    pico_proxy -->|"ログ記録"|log_files
    win_proxy -->|"ログ記録"|log_files
    dns_resolver -->|"独自名前解決"|target_hosts
    proxy_discovery -.->|"プロキシ発見"|other_proxies
    http_gateway -.->|"レコード同期"|other_proxies
    pico_proxy -->|"ネットワーク接続"|wifi_network
    win_proxy -->|"ネットワーク接続"|wifi_network
```